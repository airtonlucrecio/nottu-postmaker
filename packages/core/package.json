{
  "name": "@nottu/core",
  "version": "1.0.0",
  "description": "Core types, DTOs and utilities for Nottu PostMaker",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npm run clean && tsc --skipLibCheck --noEmitOnError false",
    "types": "tsc --emitDeclarationOnly",
    "dev": "tsc --watch",
    "clean": "rm -rf dist || true"
  },
  "dependencies": {
    "class-transformer": "^0.5.1",
    "class-validator": "^0.14.0"
  },
  "devDependencies": {
    "@types/node": "^24.9.1",
    "esbuild": "^0.19.11",
    "rimraf": "^5.0.5",
    "typescript": "^4.9.5"
  },
  "peerDependencies": {
    "typescript": "^4.9.0"
  }
}
