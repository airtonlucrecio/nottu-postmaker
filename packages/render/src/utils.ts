export function formatImage(data: Buffer, format: string): Buffer {
  // Mock implementation
  return data;
}

export function validateRenderOptions(options: any): boolean {
  // Mock implementation
  return true;
}