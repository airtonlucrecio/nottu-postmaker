{
  "name": "@nottu/brand-kit",
  "version": "1.0.0",
  "description": "Brand kit and design system for Nottu PostMaker",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npm run clean && esbuild src/index.ts --bundle --platform=node --format=cjs --sourcemap --target=node18 --outdir=dist --external:@nottu/* && npm run types",
    "types": "npm run clean && tsc --emitDeclarationOnly",
    "types": "tsc --emitDeclarationOnly",
    "dev": "tsc --watch",
    "clean": "rm -rf dist || true"
  },
  "dependencies": {
    "@nottu/core": "file:../core"
  },
  "devDependencies": {
    "typescript": "^5.3.2",
    "rimraf": "^5.0.5"
  }
}