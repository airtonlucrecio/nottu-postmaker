{
  "name": "@nottu/brand-kit",
  "version": "1.0.0",
  "description": "Brand kit and design system for Nottu PostMaker",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npm run clean && tsc",
    "types": "tsc --emitDeclarationOnly",
    "dev": "tsc --watch",
    "clean": "rm -rf dist || true"
  },
  "dependencies": {
    "@nottu/core": "file:../core"
  },
  "devDependencies": {
    "esbuild": "^0.19.11",
    "typescript": "^4.9.5",
    "rimraf": "^5.0.5"
  }
}