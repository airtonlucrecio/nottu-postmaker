{
  "name": "@nottu/queue",
  "version": "1.0.0",
  "description": "Queue management with BullMQ for Nottu PostMaker",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "npm run clean && node build.js && npm run types",
    "types": "tsc --emitDeclarationOnly",
    "dev": "tsc --watch",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@nestjs/common": "^10.2.8",
    "@nottu/core": "file:../core",
    "bullmq": "^5.61.2",
    "ioredis": "^5.3.2",
    "pino": "^8.16.2"
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "esbuild-wasm": "^0.19.11",
    "rimraf": "^5.0.5",
    "typescript": "^5.3.2"
  }
}
